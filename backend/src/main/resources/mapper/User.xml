<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="kr.com.backend.mapper.UserMapper">

    <select id="selectUser" resultType="kr.com.backend.vo.UserVO" parameterType="string">
        SELECT
            USER_ID
             , PASSWORD
             , USER_NAME
        FROM
            TB_USER
        WHERE
            USER_ID = #{userId}
    </select>


    <!-- 회원 가입 (일반) -->
    <insert id="insertUser" parameterType="kr.com.backend.vo.UserVO">
        INSERT INTO TB_USER (
                              USER_ID
                            , PASSWORD
                            , USER_NAME
                            , USER_AUTH
        ) VALUES (
                   #{userId}
                 , #{password}
                 , #{userName}
                 , 2
                 )
    </insert>

</mapper>